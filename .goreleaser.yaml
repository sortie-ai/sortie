# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: sortie
report_sizes: true

before:
  hooks:
    - go mod verify

builds:
  - id: sortie
    main: ./cmd/sortie
    binary: sortie
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - >-
        -s -w
        -X main.Version={{ .Version }}
        -X main.Commit={{ .FullCommit }}
        -X main.Date={{ .CommitDate }}
    flags:
      - -trimpath
    tags:
      - osusergo
      - netgo
    mod_timestamp: "{{ .CommitTimestamp }}"
    hooks:
      post:
        # Authenticode-sign Windows binaries with SignPath before archiving, so
        # the published archive and checksums cover the signed binary. The
        # script no-ops on non-Windows targets and when SignPath credentials are
        # absent (snapshots, pull requests, local builds).
        - cmd: ./scripts/signpath-sign.sh "{{ .Path }}"
          output: true

archives:
  - id: default
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE*
      - README*

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

sboms:
  - artifacts: archive

homebrew_casks:
  - name: sortie
    binaries:
      - sortie
    repository:
      owner: sortie-ai
      name: homebrew-tap
      branch: main
      token: "{{ .Env.TAP_GITHUB_TOKEN }}"
    directory: Casks
    commit_author:
      name: goreleaserbot
      email: goreleaserbot@users.noreply.github.com
    commit_msg_template: "chore(cask): update {{ .ProjectName }} to {{ .Tag }}"
    homepage: "https://github.com/sortie-ai/sortie"
    description: "Spec-first orchestration service for coding agents"
    skip_upload: "auto"
    url:
      verified: "github.com/sortie-ai/sortie/"
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr",
              args: ["-dr", "com.apple.quarantine", "#{staged_path}/sortie"],
              must_succeed: false
          end

changelog:
  sort: asc
  use: git
  groups:
    - title: Features
      regexp: '^.*?feat(\([^)]+\))??!?:.+$'
      order: 0
    - title: Bug Fixes
      regexp: '^.*?fix(\([^)]+\))??!?:.+$'
      order: 1
    - title: Performance
      regexp: '^.*?perf(\([^)]+\))??!?:.+$'
      order: 2
    - title: Others
      order: 999
  filters:
    exclude:
      - "(?i)^docs[:(]"
      - "(?i)^tests?[:(]"
      - "(?i)^chore[:(]"
      - "(?i)^ci[:(]"
      - "(?i)^style[:(]"
      - "(?i)^refactor[:(]"
      - "(?i)^Merge pull request #"
      - "(?i)^Merge (remote-tracking )?branch '"

release:
  github:
    owner: sortie-ai
    name: sortie
  name_template: "{{ .ProjectName }} {{ .Version }}"
  # Contributor sections come from scripts/release-contributors.sh. envOrDefault,
  # not .Env: templates run with missingkey=error, so a run without those
  # variables would fail instead of dropping the sections.
  footer: |
    {{ envOrDefault "RELEASE_NEW_CONTRIBUTORS" "" }}

    ---

    **Full changelog**: https://github.com/sortie-ai/sortie/blob/{{ .Tag }}/CHANGELOG.md

    **Diff**: https://github.com/sortie-ai/sortie/compare/{{ .PreviousTag }}...{{ .Tag }}

    {{ envOrDefault "RELEASE_CONTRIBUTORS" "" }}
  draft: false
  prerelease: auto
  make_latest: true
  mode: keep-existing

snapshot:
  version_template: "{{ incpatch .Version }}-SNAPSHOT-{{ .ShortCommit }}"
